import config from '../config';

export const urls = {
  baseUrl: config.API_BASE_URL,
  users: 'users',
  login: 'login',
};

export const sliceNames = {
  login: 'login',
};

export const dateFormats = {
  DailyDate: 'MMM D',
};
